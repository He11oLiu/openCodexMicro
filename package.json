{
  "name": "open-codex-micro",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Turn a Ulanzi D200 into openCodexMicro",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "python3 -m unittest discover -s test -p '*_test.py'",
    "test:node": "node --test test/*_test.mjs",
    "build:bridge": "node scripts/build-bridge.mjs",
    "check:node": "node --check scripts/install.mjs && node --check scripts/uninstall.mjs && node --check scripts/build-bridge.mjs && node --check src/bridge/thread-key.mjs && node --check src/bridge/codex-cdp.mjs && node --check src/bridge/server.mjs",
    "check": "npm run test && npm run test:node && npm run check:node && npm run build:bridge",
    "setup": "node scripts/install.mjs",
    "install:native": "node scripts/install.mjs",
    "bridge:start": "open \"$HOME/Applications/Codex Bridge.app\"",
    "uninstall": "node scripts/uninstall.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "ws": "^8.21.1"
  }
}
